API Reference
=============

The API reference documents the public objects exported by
``compresso_recsys`` and its public dataset, checkpoint, sequence, model,
retrieval, metric, evaluation, and statistics submodules.

.. toctree::
   :maxdepth: 2

   compresso_recsys
   datasets
   checkpoint
   sequences
   models
   retrieval
   evaluation
   statistics
